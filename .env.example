# Environment variables — copy to .env and fill in your keys
# Never commit .env to source control

# Online STT API key (only needed when online_engine.enabled = true in config.json)
ONLINE_STT_API_KEY=your_api_key_here
